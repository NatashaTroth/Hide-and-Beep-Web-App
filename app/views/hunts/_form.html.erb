<%= form_with(model: hunt, local: true) do |form| %>
  <% if hunt.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(hunt.errors.count, "error") %> prohibited this hunt from being saved:</h2>

      <ul>
      <% hunt.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :start_date %>
    <%= form.datetime_select :start_date %>
  </div>

  <div class="field">
    <%= form.label :expiry_date %>
    <%= form.datetime_select :expiry_date %>
  </div>

  <div class="field">
    <%= form.label :set_time_limit %>
    <%= form.time_select :set_time_limit %>
  </div>

  <div class="field">
    <%= form.label :no_time_limit %>
    <%= form.check_box :no_time_limit %>
  </div>

  <div class="field">
    <%= form.label :winning_code %>
    <%= form.text_field :winning_code %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
