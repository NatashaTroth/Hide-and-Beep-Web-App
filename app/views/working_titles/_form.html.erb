<%= form_with(model: working_title, local: true) do |form| %>
  <% if working_title.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(working_title.errors.count, "error") %> prohibited this working_title from being saved:</h2>

      <ul>
      <% working_title.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
